# Ενεργοποίηση RSVP — οδηγός (≈5 λεπτά)

Η φόρμα RSVP είναι έτοιμη. Μένει να τη συνδέσεις με ένα **Google Sheet**, ώστε οι
απαντήσεις να αποθηκεύονται και το ζευγάρι να βλέπει **ποιοι έρχονται και ποιοι όχι**.
Όλα δωρεάν, χωρίς server.

## Τι θα έχεις στο τέλος
- Ένα Google Sheet με μία γραμμή ανά καλεσμένο: `Ημ/νία · Όνομα · Θα έρθει; · Άτομα · Συνοδοί · Token`.
- Φύλλο **«Σύνολο»** που δείχνει ζωντανά: πόσα «Ναι», πόσα «Όχι», σύνολο ατόμων.
- **Email** σε κάθε νέα/ενημερωμένη απάντηση.
- **Dedup**: αν κάποιος στείλει 2 φορές, ενημερώνεται η ίδια γραμμή (όχι διπλή).
- **Anti-spam** honeypot (ήδη στη φόρμα).

---

## Βήματα

### 1. Φτιάξε το Google Sheet
Πήγαινε στο <https://sheets.google.com> → **Κενό υπολογιστικό φύλλο**. Ονόμασέ το π.χ. «RSVP — Άννα & Δημήτρης».

### 2. Άνοιξε τον editor του Apps Script
Στο Sheet: **Επεκτάσεις → Apps Script** (Extensions → Apps Script).

### 3. Κόλλα τον κώδικα
Σβήσε ό,τι υπάρχει στο `Code.gs` και κόλλα **όλο** το περιεχόμενο του αρχείου
[`Code.gs`](./Code.gs) αυτού του project.

### 4. Βάλε το email σου
Στην κορυφή του κώδικα, άλλαξε τη γραμμή:
```js
var NOTIFY_EMAIL = "PASTE_EMAIL";
```
σε π.χ. `var NOTIFY_EMAIL = "onoma@gmail.com";`
(Άφησέ το `"PASTE_EMAIL"` αν δεν θες emails.) Πάτα **💾 Save**.

### 5. Τρέξε μία φορά τη `setup()` (για να δώσεις άδειες)
Πάνω-πάνω, στο dropdown των συναρτήσεων διάλεξε **`setup`** → **Run**.
Θα σου ζητήσει άδεια (Authorize) — δώσε **Allow** στον λογαριασμό σου.
Έτσι φτιάχνονται τα φύλλα «Σύνολο» + «Απαντήσεις» και επιτρέπεται η αποστολή email.

### 6. Κάνε Deploy ως Web app
- Πάνω δεξιά **Deploy → New deployment**.
- Γρανάζι ⚙️ → τύπος **Web app**.
- **Execute as:** *Me* (εσύ).
- **Who has access:** **Anyone** ← σημαντικό, αλλιώς η φόρμα δεν μπορεί να στείλει.
- **Deploy** → Authorize αν ξαναζητηθεί → **αντίγραψε το «Web app URL»**
  (κάτι σαν `https://script.google.com/macros/s/AKfy…/exec`).

### 7. Σύνδεσε το URL στο site
Άνοιξε το `index.html`, βρες κάτω-κάτω το `CONFIG` και άλλαξε:
```js
endpoint: "PASTE_YOUR_APPS_SCRIPT_URL_HERE",
```
σε:
```js
endpoint: "https://script.google.com/macros/s/AKfy…/exec",
```
(ή στείλ' μου το URL και το κάνω εγώ.) Η φόρμα βγαίνει αυτόματα από demo mode.

---

## Δοκιμή
1. Άνοιξε το site, συμπλήρωσε μια δοκιμαστική απάντηση, πάτα **Επιβεβαίωση απάντησης**.
2. Δες μια νέα γραμμή στο φύλλο «Απαντήσεις» και το «Σύνολο» να ανεβαίνει.
3. Αν έβαλες email, θα λάβεις ειδοποίηση.
4. Επαλήθευση endpoint: άνοιξε το Web app URL σε καρτέλα — δείχνει «✅ RSVP endpoint ενεργό…».

## Χρήσιμα
- **Προσωπικά links καλεσμένων** (προ-συμπληρωμένο όνομα + σταθερό dedup):
  `…/index.html?to=Μαρία%20Παπαδοπούλου&t=maria01`
  Το `t` είναι μοναδικό ανά καλεσμένο (π.χ. `maria01`) — αν ξαναστείλει, ενημερώνεται η ίδια γραμμή.
- **Αν αλλάξεις τον κώδικα** στο Apps Script: **Deploy → Manage deployments → ✏️ → Version: New version → Deploy** (το URL μένει ίδιο).
- Το ζευγάρι βλέπει τα πάντα μοιράζοντας απλώς το Google Sheet (Share) — δεν χρειάζεται να ξέρει τίποτα τεχνικό.

## Εκκρεμότητες που βελτιώνουν το calendar/χάρτη
- `venueName` + `mapUrl` στο `CONFIG` είναι ακόμη placeholders. Μόλις μπει το όνομα/διεύθυνση
  της αίθουσας, το κουμπί «Προσθήκη στο ημερολόγιο» και ο χάρτης δείχνουν ακριβή τοποθεσία.
